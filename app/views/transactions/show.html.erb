<div class="container my-5">
  <% if notice.present? %>
    <div class="alert alert-success" role="alert">
      <%= notice %>
    </div>
  <% end %>

  <%= render @transaction %>

  <div class="mt-3">
    <%= link_to "Voltar para o Usuário", user_path(@transaction.wallet.user), class: "btn btn-outline-secondary" %>
  </div>
</div>
